---
const {title, image, span} = Astro.props;
---

<img
  class='w-full select-none object-cover first:row-span-2 first:aspect-[1/2.04] aspect-square transition-transform duration-300 active:scale-150'
  src={image}
  alt={title}
  decoding='async'
  loading='lazy'
/>
